K2_API_KEY=
PORT=5000
# Comma-separated origins allowed to call POST /api/knot/session (match dev:frontend port)
FRONTEND_ORIGIN=http://localhost:3000
# Or list your ngrok page URL explicitly, e.g. https://abc123.ngrok-free.dev
# Optional dev-only: allow any ngrok browser tunnel origin without editing FRONTEND_ORIGIN each time
# CORS_ALLOW_NGROK=true